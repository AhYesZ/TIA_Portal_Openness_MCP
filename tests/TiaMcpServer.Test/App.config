<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <!--
    Siemens.Engineering.* assemblies are resolved at runtime by
    Siemens.Collaboration.Net.TiaPortal.Openness.Resolver, registered via
    Openness.Initialize() in AssemblyHooks.cs. The previous hardcoded
    privatePath probe pointed to a V20 path that never worked anyway
    (privatePath only resolves against AppBase subfolders, not absolute paths).
  -->
</configuration>